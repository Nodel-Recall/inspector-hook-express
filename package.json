{
  "name": "inspector-hook-express",
  "version": "0.1.0",
  "description": "A tool to inspect webhooks",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "npm run build:ui && NODE_ENV=development nodemon index.js",
    "lint": "eslint .",
    "build:ui": "rm -rf build && cd ../inspector-hook-frontend && npm run build && cp -r build ../inspector-hook-express"
  },
  "keywords": [
    "webhook"
  ],
  "author": "Nodal Recall",
  "license": "GPL-3.0-or-later",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "morgan": "^1.10.0",
    "pg": "^8.10.0",
    "json-stringify-safe": "5.0.1",
    "uuid": "9.0.0",
    "socket.io": "4.6.1"
  },
  "devDependencies": {
    "eslint": "^8.32.0",
    "nodemon": "^2.0.20"
  }
}
